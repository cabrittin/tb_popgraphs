#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='tb_popgraphs',
      version='1.0',
      description='General use tool box',
      author='cabrittin',
      author_email='cabrittin at the google',
      url='git@github.com:cabrittin/tb_popgraph.git',
      packages=['tb_popgraphs','tb_popgraphs.generators','tb_popgraphs.graphs'],
     )
